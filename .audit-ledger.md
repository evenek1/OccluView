# Реестр находок и правок (рабочий, не коммитить)

Ветка `zer0ltrnce/1.21`, HEAD `b8299b3` + незакоммиченные правки ниже.
Источник: 14 линий аудита (runs 9679e4fe, 38623e5a…f8c59228) + прошлые волны.
Полные тексты: `/tmp/findings-*.md`, память аудита `/tmp/OCCLUVIEW-AUDIT-MEMORY.md`,
диагноз Align `/tmp/ALIGN-DIAGNOSIS.md`.

## Сделано и проверено тестами

- [x] **Align P0 закрыт**: 8/8 корпусных тестов, ошибка 0.000 мм, trusted=true. Коммит 6043252.
- [x] OFF: числа в строке ключевого слова (`OFF 3 1 0`) читались как пустой меш без ошибки. `formats/src/off.rs`, 3 теста.
- [x] glTF: индекс-сентинел `0xFFFFFFFF` заворачивался в `base + a` и подставлял чужую вершину (в debug — abort). `gltf/primitive.rs`, тест.
- [x] BOM UTF-8 ломал PLY («не PLY») и уводил ASCII STL в бинарный ридер. `dispatch.rs`, `ply/mod.rs`, `stl/mod.rs`, 3 теста.
- [x] Hover настроек обещал PLY вместо сохранённого формата. 7 локалей.
- [x] Утечка HGLOBAL в буфере обмена Explorer при занятом буфере (до 16 МБ в `prevhost.exe`). `shell/com/preview/context_menu.rs`.
- [x] Экспорт при живом штрихе скульптинга: один гард на все три пути, тест. Коммит fa6e6c0.
- [x] Карточка Repair Mesh стала модальной (шестой термин в `OpenDialogs`, тест перебирает все термины).
- [x] «heavy at» больше не разгоняет палитру за досягаемость зонда; легенда читает саму палитру; 2 теста.
- [x] Offscreen readback: убран лишний переворот; golden baseline перегенерирован, вершина треугольника объективно переехала в верхние строки.
- [x] `occluview-cli thumbnail`: код возврата 1 для placeholder, контракт PNG сохранён, тест обновлён.
- [x] Упавший align-воркер больше не залипает: `align_worker_mut` заменяет мёртвый, тест на респавн.

## Align (P0) — ЗАКРЫТ (коммит 6043252)

Правки: плотность посадки (`seated_fraction`, полоса 0.05 мм) как первичная ось в приёмке шага,
выборе лучшей позы и ранжировании грубых кандидатов; в сиде по фичам сравнение гипотез и «равный
соперник» по плотной полосе (`TIGHT_CONSENSUS_MM`) вместо грубых 0.5 мм; порог поддержки сида 12.
Ключевые замеры задокументированы в коде. Разбор: /tmp/ALIGN-DIAGNOSIS.md.

## P1 — осталось

- [ ] Штрих скульптинга стартует на удержанной кнопке: Ctrl+Z во время протяжки откладывается и срабатывает не по тому штриху. `app/app_sculpt.rs:314-329`, `app_mesh_editor.rs:426-441`, `app_sculpt_worker.rs:59-68`.
- [ ] `occluview-cli close-holes` закрывает то, что кнопка в приложении отказывается закрывать. `cli/src/*`.
- [ ] Единственный end-to-end тест контактов всегда скипается, гейта принудительного запуска нет.
- [ ] Undo во время удержанного перетаскивания сцены пишет шаг истории против сцены, которую вот-вот выбросят. `app/app_*` (flows).

## P1 в плане, но требует решения владельца (не код)

- [ ] Пин оболочки Explorer: MSI/zip несут закреплённую ревизию v1.1.0 (контракт), отставание 112 коммитов — не баг.

## P2 — не начато (по линиям)

formats-read: `.gltf` (JSON) пробрасывается как glTF и отвергается «not a glTF»; резервирование OFF ограничено числом элементов, а не байтами.
formats-write: PLY-писатель встраивает текстуру без ограничения размера, читатель её потом отвергает (>64 МиБ эквивалент) — экспорт рапортует успех, текстура теряется.
frame-state: поповеры закрываются по Escape без его потребления (одно нажатие гасит и меню, и инструмент); лассо потребляет Enter/Escape мимо модального предиката.
app-tools: Single-pass Separate дублирует вершину при чередовании патча и остатка (рвёт меш); «Mark automatic» рапортует только про диски-стрелки.
contact: упакованное контактное поле рассчитано на запрошенный лимит текстуры (8192), а не на выданный устройством.
render: живое устройство ограничено `max_buffer_size` 256 МиБ (offscreen уже поднят); радиус сплаттера считается от клампленного `render_extent_px`; doc `GpuContactMaterial` противоречит коду.
shell: `DllRegisterServer` пишет ProgID `MeshFile.DCM`, а MSI/.reg/тесты говорят `MeshFile.HPS` (двойная запись в «Открыть с помощью»); «Copy image» рендерит в размере панели вместо 2048; COM-классы заявляют agility поверх `RefCell`/`Cell` (ломает STA-сериализацию).
geometry: гейт rim-simplicity — O(n²) без бюджета на 20 000 рёбер; страж «unweldable soup» отключается `heal_boundary_rims`, и лечащий проход удаляет геометрию.
concurrency: отменённая задача Bridge Split продолжает считать и блокирует следующий превью; настройки кисти скульптинга теряются при закрытии в пределах 1 с (debounce); прогретый в фоне BVH не мешает построить его в UI-потоке.
cli/thumb: неявное имя миниатюры уступает один раз, потом молча заменяет файл; вердикт «повреждённая геометрия» для .glb/.hps/.dcm/.3mf/.off рапортуется как «STL»; `thumbnail_timeout::run_with_timeout` — мёртвый код.
i18n/docs: glTF с единицами «метры» импортируется в сыром масштабе, интерпретация единиц нигде не читается; шесть английских ключей без вызовов (в т.ч. единственное объяснение «Area each side»).
tests: гейт CHANGELOG «секция для нетегированной версии» недостижим в CI.

## Известный долг из прошлых волн (не переоткрывать)

- Бюджет импорта доверяет метаданным файла (документировано).
- UV-таблица и очередь `pending` в PLY-ридере не заряжены в бюджет меша.
- На GTK выбор фильтра в диалоге сохранения игнорируется для имени без расширения.
- `renamed` может посчитать один слой дважды при коллизии имён.
- 30 из 98 source-text тестов оставлены как статические швы.

## Процедура проверки

```
/home/wow/occlutraceio/scripts/heavy.sh bash -c "cargo fmt --all; cargo clippy --workspace --all-targets --locked -- -D warnings"
/home/wow/occlutraceio/scripts/heavy.sh cargo test --workspace --locked
OCCLUVIEW_ALIGN_FIXTURES=/tmp/align-corpus bash scripts/validate-release-private.sh
```
